برای اجرای این سیستم، شما با `harness.py` سر و کار دارید. این فایل هسته‌ی مرکزی است که ارتباط بین عامل A (Orchestrator) و عامل B (Worker) را مدیریت می‌کند.

### ۱. دستور اجرای ساده

بعد از اینکه فایل‌های خود را در پوشه‌ی `cli-orchestrator` قرار دادید و وابستگی‌ها را نصب کردید (`pip install -r requirements.txt`)، برای اجرای یک دستور کافیست ترمینال (یا CMD/PowerShell) را در همان پوشه باز کنید و بنویسید:

```bash
python harness.py --task "کد یک اسکریپت ساده برای اسکن پورت‌های باز بنویس"

```

### ۲. اجرای نمونه با استفاده از فایل task.txt

اگر وظیفه‌ای که دارید طولانی است یا نمی‌خواهید مستقیم در خط فرمان تایپ کنید، می‌توانید آن را داخل فایل `task.txt` بنویسید و سپس فقط دستور زیر را اجرا کنید:

```bash
python harness.py

```

سیستم به طور خودکار محتویات `task.txt` را می‌خواند و عملیات را شروع می‌کند.

### ۳. اجرای نمونه با تنظیمات خاص (تست کردن)

اگر می‌خواهید برای تست کردن، از تنظیمات خاصی که برای `config` ساختید استفاده کنید:

```bash
# اجرای برنامه با یک تنظیمات خاص
python harness.py --config config.mock.yaml --task "یه دیتابیس ساده با SQLite بساز"

```

---

### این سیستم در پس‌زمینه چه می‌کند؟ (نمای کلی معماری)

وقتی شما دستور بالا را می‌زنید، این اتفاقات می‌افتد:

1. **شروع:** هارنس فایل `config.yaml` را می‌خواند تا بفهمد کدام CLIها برای نقش‌های A و B تنظیم شده‌اند.
2. **چرخه (Loop):** - ابتدا به عامل A دستور می‌دهد که `task` شما را بررسی کند.

- عامل A (مثلاً Antigravity) تحلیل می‌کند و پرامپت را بهینه‌سازی کرده و آن را به صورت یک درخواست `delegate` به هارنس پس می‌دهد.
- هارنس این درخواست را به عامل B (مثلاً Codex) می‌فرستد.
- عامل B خروجی را می‌سازد و به هارنس برمی‌گرداند.
- هارنس نتیجه را به A می‌دهد تا ببیند آیا کار تمام شده یا نیاز به اصلاح بیشتر دارد.

3. **پایان:** اگر عامل A بگوید `finalize` (یعنی کار تمام شد)، هارنس نتیجه نهایی را در یک فایل متنی ذخیره کرده و به شما اعلان (Toast/Sound) می‌دهد.

### چند نکته برای راحتی کار:

- **لاگ‌ها:** بعد از هر اجرا، یک پوشه جدید در `logs/` ساخته می‌شود. اگر سیستم متوقف شد یا خطایی داد، حتماً فایل `harness.log` را در آن پوشه چک کنید.
- **توقف سریع:** در هر لحظه که خواستید اجرای زنجیره‌ای متوقف شود، کافیست `Ctrl+C` را در ترمینال بزنید تا هارنس تمامی پردازش‌های فرزند (CLIها) را به صورت ایمن ببندد.
- **اعلان‌ها:** اگر ویندوز دارید، وقتی کار تمام شود یک نوتیفیکیشن در پایین سمت راست صفحه ظاهر می‌شود که می‌گوید کار با موفقیت انجام شد یا اگر خطایی مثل `Rate Limit` رخ داد، بلافاصله به شما خبر می‌دهد.
